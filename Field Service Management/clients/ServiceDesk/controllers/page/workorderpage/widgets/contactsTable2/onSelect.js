page.data.selectedContacts.push(extra);

//* CONTACTS
/*var myArray = page.data.Workorders.contacts;
//var table = client.getWidget("contactsTable");
//selectedrow = table.selectedRowObject;

for (var i =0; i < myArray.length; i++)
   if (myArray[i].lastname === extra.lastname && myArray[i].firstname === extra.firstname) {
      myArray.splice(i,1);
      break;
   }

page.data.Workorders.contacts = myArray;
client.sendClientEvent("ce_Contacts", page.data.Workorders.contacts);
*/